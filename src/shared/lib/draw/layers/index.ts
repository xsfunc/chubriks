import { drawBackground } from './background'
import { drawHead, drawHeadStroke } from './head'
import { drawEyes, drawMouth } from './face'

export const layers = {
  drawBackground,
  drawHead,
  drawHeadStroke,
  drawEyes,
  drawMouth,
}
